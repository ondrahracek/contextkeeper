# ContextKeeper

A Go CLI tool for managing context and configuration.

## Usage

```bash
contextkeeper [command]
```

## Commands

- `init` - Initialize contextkeeper
- `add` - Add a new item
- `list` - List all items
- `remove` - Remove an item
